# Local production test: docker compose up --build
# App: http://localhost:8000

services:
  recipeai:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    environment:
      DATA_DIR: /data
      PORT: "8000"
    volumes:
      - recipeai-data:/data
    restart: unless-stopped

volumes:
  recipeai-data:
